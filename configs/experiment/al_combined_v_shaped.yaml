# @package _global_

# Combined V-shaped strategy: Entropy + CP V-shaped
defaults:
  - override /al: combined_v_shaped

# Experiment name
task_name: "al_combined_v_shaped"

# Tags
tags: ["combined", "entropy_cp_v"]

# AL strategy settings
al:
  strategy: "combined_v_shaped"
  num_rounds: 20
  budget_per_round: 2000
