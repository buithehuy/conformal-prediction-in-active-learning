# @package _global_

# Combined strategy: Entropy + CP set size
defaults:
  - override /al: combined

# Experiment name
task_name: "al_combined"

# Tags
tags: ["combined", "entropy_cp"]

# AL strategy settings
al:
  strategy: "combined"
  num_rounds: 20
  budget_per_round: 2000
