# @package _global_

# Least confidence sampling strategy

# Experiment name
task_name: "al_least_confidence"

# Tags
tags: ["uncertainty", "least_confidence"]

# AL strategy settings
al:
  strategy: "least_confidence"
  num_rounds: 20
  budget_per_round: 2000
