# @package _global_

# Conformal Prediction set size sampling

# Experiment name
task_name: "al_cp_size"

# Tags
tags: ["conformal_prediction", "cp_size"]

# AL strategy settings
al:
  strategy: "cp_size"
  num_rounds: 20
  budget_per_round: 2000
